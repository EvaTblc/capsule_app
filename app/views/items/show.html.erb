<div class="min-h-screen p-4 pt-20 pb-24">

  <% style = Item::ITEM_TYPE_STYLES[@item.item_type] %>
  <% icon = Item::TYPE_TO_ICON[@item.item_type] %>

<div class="rounded-3xl mb-6 flex flex-col justify-end relative overflow-hidden <%= @collection.cover_image.attached? ? 'h-48' : style + ' p-6' %>"
     style="<%= @collection.cover_image.attached? ? "background-image: url('#{url_for(@collection.cover_image)}'); background-size: cover; background-position: center;" : "" %>">

  <% if @collection.cover_image.attached? %>
    <div class="absolute inset-0 bg-black opacity-40 rounded-3xl"></div>
  <% end %>

  <div class="relative z-10 <%= @collection.cover_image.attached? ? 'p-6' : '' %> h-full flex flex-col justify-between">
    <div class="w-14 h-14 bg-white rounded-2xl flex items-center justify-center shadow-sm">
      <%= render "collections/icons/#{icon}" %>
    </div>
    <div class="flex flex-col gap-1">
      <p class="text-xs font-semibold uppercase tracking-widest <%= @collection.cover_image.attached? ? 'text-white opacity-75' : 'text-gray-400' %>"><%= @item.item_type.gsub('Detail', '').titleize %></p>
      <p class="text-xs <%= @collection.cover_image.attached? ? 'text-white opacity-75' : 'text-gray-400' %>"><%= @item.condition %></p>
      <div class="flex items-center justify-between">
        <h1 class="text-3xl font-black tracking-tight <%= @collection.cover_image.attached? ? 'text-white' : '' %>"><%= @item.title %></h1>
        <%= link_to edit_collection_item_path(@collection, @item), class: "w-9 h-9 bg-white rounded-xl flex items-center justify-center shadow-sm flex-shrink-0" do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
          </svg>
        <% end %>
      </div>
      <% link_color = @collection.cover_image.attached? ? 'text-white opacity-75' : 'text-gray-400' %>
      <%= link_to @collection, class: "flex items-center gap-1 text-xs #{link_color} w-fit" do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
        </svg>
        <%= @collection.title %>
      <% end %>
    </div>
  </div>
</div>


  <div class="bg-white border border-gray-100 rounded-2xl p-4 flex flex-col gap-3">
    <% @item.item_detailable.attributes.except("id", "created_at", "updated_at").each do |key, value| %>
      <% next if value.blank? %>

      <% if key == "description" %>
        <div class="flex justify-between items-start border-b border-gray-50 pb-2" data-controller="expandable">
          <p class="text-xs font-semibold text-gray-400 uppercase">Description</p>
          <div class="flex flex-col items-end gap-1 max-w-xs">
            <p class="text-sm font-medium text-right line-clamp-3" data-expandable-target="content"><%= value %></p>
            <button data-expandable-target="button" data-action="click->expandable#toggle" class="text-xs text-purple-600 font-semibold">Voir plus</button>
          </div>
        </div>
      <% else %>
        <div class="flex justify-between items-center border-b border-gray-50 pb-2">
          <p class="text-xs font-semibold text-gray-400 uppercase"><%= key.humanize %></p>
          <p class="text-sm font-medium"><%= value %></p>
        </div>
      <% end %>
    <% end %>
  </div>

  <% if @item.images.attached? %>
    <div class="grid grid-cols-2 gap-2 mb-6 mt-6">
      <% @item.images.each do |image| %>
        <div class="h-40 rounded-2xl overflow-hidden">
          <%= image_tag image, class: "w-full h-full object-cover" %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
